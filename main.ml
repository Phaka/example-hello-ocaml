(* Hello World in OCaml *)
(* OCaml uses static typing and type inference *)
(* The print_endline function returns unit type - similar to void *)
(* OCaml programs are expressions that evaluate to values *)

print_endline "Hello, World!"
